<ul class="groups container">
  <% groups.each do |group| %>
      <li><%= link_to group.title, group_url(group.code) %></li>
  <% end %>
  <li><%= link_to t('groups.new_group'), new_group_url %></li>
</ul>
