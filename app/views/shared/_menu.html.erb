<ul class="links container">
  <% items.each do |item| next if item[:disabled] %>
      <li>
        <%= link_to item[:text], item[:url], :method => item[:method] || 'get' %>
      </li>
  <% end %>
</ul>
